{# src/layouts/base.html.twig #}
{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12 ml-md-auto" style="background: white;color: black;">
                <h1>Edit Theme</h1>

                {{ include('theme/_form.html.twig', {'button_label': 'Update'}) }}

                <a class="btn btn-primary" href="{{ path('theme_index') }}">back to list</a>

                {{ include('theme/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
