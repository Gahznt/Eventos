{# src/layouts/base.html.twig #}
{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <nav>
            <div class="nav nav-tabs" id="nav-tab" role="tablist">
                <a class="nav-item nav-link active" href="{{ path('manager_payment_user_associations_index') }}"
                   role="tab">
                    {{ 'Associação de Pagamento de Usuário'|trans }}
                </a>
            </div>
        </nav>
        <div class="tab-content">
            <div class="tab-pane fade show active" role="tabpanel">
                <nav>
                    <div class="nav nav-tabs" role="tablist">
                        <a id="manager-back-route" class="nav-item nav-link"
                           href="{{ path('manager_payment_user_associations_index') }}" role="tab">
                            {{ 'Lista de Associação<br>de Pagamento de Usuário'|trans|raw }}
                        </a>
                        <a class="nav-item nav-link active" href="#dados" role="tab">
                            {{ 'Adicionar Associação<br>de Pagamento de Usuário'|trans|raw }}
                        </a>
                    </div>
                </nav>
                <div class="tab-content">
                    <div class="tab-pane fade show active" role="tabpanel">
                        <h1>{{ 'Adicionar Associação de Pagamento de Usuário'|trans }}</h1>

                        {% include "gestor/tabs/payment_user_associations/partials/_form.html.twig" %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('paymentUserAssociation') }}
{% endblock %}
